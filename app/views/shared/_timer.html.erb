<div class="timer" data-end-time= "<%= @test_passage.timer_finish.to_s %>"
     data-passage-id = "<%= @test_passage.id %>" >

  <span class="minutes"></span>
  <strong> minutes </strong>
  <span class="seconds"></span>
  <strong> seconds </strong>

</div>