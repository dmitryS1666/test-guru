<progress max="100" value="0" class="progress-bar"
          data-questions="<%= Rails.cache.read("test_#{@test_passage.test.id}") %>"
          data-question-index="<%= current_question(@test_passage) %>">
</progress>